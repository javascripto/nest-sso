<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home - NestJS SSO</title>
    <link rel="stylesheet" href="/styles.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
</head>
<body class="font-inter bg-gradient-to-br from-indigo-500 to-purple-600 min-h-screen flex items-center justify-center">
    <div class="bg-white p-12 rounded-2xl shadow-2xl w-full max-w-lg text-center">
        <div class="inline-flex items-center gap-2 bg-gradient-to-br from-emerald-500 to-emerald-600 text-white py-2 px-4 rounded-full text-xs font-semibold uppercase tracking-wider mb-6">
            <span class="w-2 h-2 bg-white rounded-full animate-pulse-custom"></span>
            Conectado
        </div>
        
        <% if (user.picture) { %>
            <img src="<%= user.picture %>" alt="Avatar" class="w-24 h-24 rounded-full mx-auto mb-6 border-4 border-white shadow-lg shadow-indigo-400/40">
        <% } else { %>
            <div class="w-24 h-24 rounded-full bg-gradient-to-br from-indigo-500 to-purple-600 flex items-center justify-center mx-auto mb-6 text-4xl font-bold text-white border-4 border-white shadow-lg shadow-indigo-400/40">
                <%= user.firstName.charAt(0).toUpperCase() %><%= user.lastName ? user.lastName.charAt(0).toUpperCase() : '' %>
            </div>
        <% } %>
        
        <h1 class="text-slate-900 text-3xl font-bold mb-2">Olá, <%= user.firstName %>!</h1>
        <p class="text-gray-500 text-base mb-8"><%= user.email %></p>
        
        <div class="bg-slate-50 rounded-xl p-6 mb-6 text-left">
            <h3 class="text-gray-700 text-sm font-semibold uppercase tracking-wide mb-4 flex items-center gap-2">
                <svg class="w-5 h-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"></path>
                    <circle cx="12" cy="7" r="4"></circle>
                </svg>
                Informações da Conta
            </h3>
            <div class="flex justify-between py-3 border-b border-gray-200">
                <span class="text-gray-500 text-sm">Nome</span>
                <span class="text-slate-900 text-sm font-medium"><%= user.firstName %></span>
            </div>
            <div class="flex justify-between py-3 border-b border-gray-200">
                <span class="text-gray-500 text-sm">Sobrenome</span>
                <span class="text-slate-900 text-sm font-medium"><%= user.lastName || '-' %></span>
            </div>
            <div class="flex justify-between py-3 border-b border-gray-200">
                <span class="text-gray-500 text-sm">Email</span>
                <span class="text-slate-900 text-sm font-medium"><%= user.email %></span>
            </div>
            <div class="flex justify-between py-3">
                <span class="text-gray-500 text-sm">Status</span>
                <span class="inline-flex items-center gap-1.5 text-emerald-500 text-xs font-medium">
                    <span class="w-2 h-2 bg-emerald-500 rounded-full animate-pulse-custom"></span>
                    Autenticado
                </span>
            </div>
        </div>
        
        <div class="flex gap-3 justify-center">
            <a href="/auth/profile" class="inline-flex items-center justify-center gap-2 py-3.5 px-7 rounded-xl text-sm font-semibold no-underline transition-all duration-200 bg-gradient-to-br from-indigo-500 to-purple-600 text-white hover:-translate-y-0.5 hover:shadow-lg hover:shadow-indigo-500/30">
                <svg class="w-5 h-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"></path>
                    <circle cx="12" cy="7" r="4"></circle>
                </svg>
                Ver Perfil
            </a>
            <a href="/auth/logout" class="inline-flex items-center justify-center gap-2 py-3.5 px-7 rounded-xl text-sm font-semibold no-underline transition-all duration-200 bg-white text-red-500 border-2 border-red-200 hover:bg-red-50 hover:border-red-500">
                <svg class="w-5 h-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"></path>
                    <polyline points="16 17 21 12 16 7"></polyline>
                    <line x1="21" y1="12" x2="9" y2="12"></line>
                </svg>
                Sair
            </a>
        </div>
        
        <div class="mt-8 pt-6 border-t border-gray-200 text-gray-400 text-xs">
            Autenticado via Google SSO • NestJS
        </div>
    </div>
</body>
</html>
