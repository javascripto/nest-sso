<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Realizado - NestJS SSO</title>
    <link rel="stylesheet" href="/styles.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
</head>
<body class="font-inter bg-gradient-to-br from-indigo-500 to-purple-600 min-h-screen flex items-center justify-center">
    <div class="bg-white p-10 rounded-2xl shadow-2xl w-full max-w-md text-center">
        <div class="w-20 h-20 bg-gradient-to-br from-emerald-500 to-emerald-600 rounded-full flex items-center justify-center mx-auto mb-6 animate-scale-in">
            <svg class="w-10 h-10 fill-white" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                <path d="M9 16.17L4.83 12l-1.42 1.41L9 19 21 7l-1.41-1.41z"/>
            </svg>
        </div>
        
        <h1 class="text-slate-900 text-2xl font-semibold mb-2">Login Realizado!</h1>
        <p class="text-gray-500 text-sm mb-8">Você foi autenticado com sucesso</p>
        
        <div class="bg-gray-50 rounded-xl p-6 mb-6">
            <% if (user && user.picture) { %>
                <img src="<%= user.picture %>" alt="Avatar" class="w-16 h-16 rounded-full mx-auto mb-4 border-[3px] border-indigo-500">
            <% } else { %>
                <div class="w-16 h-16 rounded-full mx-auto mb-4 bg-indigo-500 flex items-center justify-center text-white text-2xl font-semibold">
                    <%= user ? user.firstName.charAt(0) : 'U' %>
                </div>
            <% } %>
            <div class="text-slate-900 text-lg font-semibold mb-1"><%= user ? `${user.firstName} ${user.lastName}` : 'Usuário' %></div>
            <div class="text-gray-500 text-sm"><%= user ? user.email : '' %></div>
        </div>
        
        <div class="flex gap-3 justify-center">
            <a href="/" class="inline-flex items-center gap-2 py-3 px-6 bg-indigo-500 text-white rounded-lg text-sm font-medium no-underline transition-all duration-200 hover:bg-indigo-600 hover:-translate-y-0.5">Ir para Home</a>
            <a href="/auth/logout" class="inline-block py-3 px-6 bg-transparent text-gray-500 border-2 border-gray-200 rounded-lg text-sm font-medium no-underline transition-all duration-200 hover:bg-gray-50 hover:border-gray-300">Sair</a>
        </div>
    </div>
</body>
</html>
